#include <stdio.h>

int main() 
{
    int numArr[10] = { 0, };    // 배열의 요소를 모두 0으로 초기화 

    printf("%d\n", numArr[0]);
    printf("%d\n", numArr[0]);
    printf("%d\n", numArr[0]);

    return 0;

}